{
  "devDependencies": {
    "sass": "^1.101.0"
  },
  "scripts": {
    "build-css": "sass assets/styles:assets/styles",
    "watch-css": "sass --watch assets/styles:assets/styles",
    "serve": "browser-sync start --server --files 'assets/styles/*.css, *.html'"
  }
}
